[workspace]
resolver = "2"
members = ["hashing"]

[workspace.package]
edition = "2021"
license = "UNLICENSED"
publish = false

# Optimize wasm artifacts for size.
[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
