# packages/infra

Infrastructure adapter packages — database, cache, email, queue, storage. They
wrap external services behind a clean interface so domain code stays portable. A
grouping namespace, not a package itself (registered as `packages/infra/*`).
