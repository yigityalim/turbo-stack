# @repo/{{ kebabCase name }} — environment variables.
# Copy to .env.local and fill. Real values are NEVER committed. NEXT_PUBLIC_*
# are auto-inferred by Turbo; declare server-only vars in turbo.json `env`.

NEXT_PUBLIC_SITE_URL="http://localhost:3000"
