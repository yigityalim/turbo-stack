# @repo/{{ kebabCase name }}

A [Hono](https://hono.dev) app on Bun, wired into the monorepo.

```sh
bun run dev --filter=@repo/{{ kebabCase name }}
```

Intentionally minimal — add routes, middleware, and your transport/auth choice
as the API grows.
