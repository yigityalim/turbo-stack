use wasm_bindgen::prelude::*;

/// TODO: replace with the crate's real API.
#[wasm_bindgen]
pub fn greet(name: &str) -> String {
    format!("Hello, {name}!")
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn greets() {
        assert_eq!(greet("world"), "Hello, world!");
    }
}
