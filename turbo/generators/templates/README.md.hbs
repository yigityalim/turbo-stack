# @repo/{{ kebabCase name }}

> TODO: describe this package.

Internal package — exported directly from `src/` (JIT, no build step). Import it
elsewhere as `@repo/{{ kebabCase name }}` with `workspace:*`.
