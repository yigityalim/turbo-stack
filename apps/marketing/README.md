# apps/marketing

A surface group — the apps that make up the public/marketing surface live here
(e.g. `web`, `auth`). This folder groups apps; it is not a package itself and is
registered as `apps/marketing/*` in the root `workspaces`.
