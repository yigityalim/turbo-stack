# @repo/marketing-admin

The admin app — a Next.js 16 (App Router) app, scaffolded with `create-next-app`
and wired into the monorepo. Intentionally blank: it could become a CMS
(Keystatic, Sanity, …) or static MDX — that choice is left to you.

```sh
bun run dev --filter=@repo/marketing-admin
```
